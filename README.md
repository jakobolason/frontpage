A simple portfolio done using leptos.

run using

```Bash
trunk serve --open
```

And build using

```Bash
trunk build --release
```

Uses k3s on my server -- cloud provider Hetzner -- where the `k8s/deployment.yaml` is used for serving the static page using nginx.
